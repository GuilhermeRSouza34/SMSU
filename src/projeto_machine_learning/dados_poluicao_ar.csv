import random
import csv

# Função para gerar dados sintéticos de poluição do ar
def gerar_dados_poluicao_ar(n):
    dados = []
    for _ in range(n):
        data = {
            'data': '2023-01-01',  # Data fictícia para simular dados históricos
            'hora': random.randint(0, 23),  # Hora do dia
            'poluicao_ar': random.uniform(0, 100)  # Índice de poluição do ar
        }
        dados.append(data)
    return dados

# Gerar 1000 registros de dados de poluição do ar
dados_poluicao_ar = gerar_dados_poluicao_ar(1000)

# Salvar os dados em um arquivo CSV
with open('dados_poluicao_ar.csv', 'w', newline='') as csvfile:
    fieldnames = ['data', 'hora', 'poluicao_ar']
    writer = csv.DictWriter(csvfile, fieldnames=fieldnames)
    writer.writeheader()
    for data in dados_poluicao_ar:
        writer.writerow(data)

print("Dados de poluição do ar gerados com sucesso!")
